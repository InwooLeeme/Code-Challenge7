doctype html
html
    head
        meta(charset='utf-8')
        title #{pageTitle}
    body
        h1 TXT2HTML!
        form(action="read", method="post", enctype="multipart/form-data")
            input(type="file", name="userText", accept="text/plain")
            input(type="submit", value="Convert!")